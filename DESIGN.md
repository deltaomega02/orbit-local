# Orbit 디자인 명세

컨셉: **스타일 저널 (Style Journal)**

매일의 착장을 번호와 날짜로 기록하는 개인 아카이브. 앱이 아니라 **내가 써 온 기록물**처럼 보여야 한다.
화면의 주인공은 UI 가 아니라 옷 사진이고, UI 는 그 사진을 감싸는 종이다.

이 문서는 취향의 기록이 아니라 **구현 기준**이다. 값이 필요하면 여기서 가져다 쓰고,
여기 없는 값을 새로 만들지 마라.

---

## 왜 이 컨셉인가

**전제 1. 사진이 예쁘지 않다.** 사용자가 올리는 건 침대 위에 펼친 옷, 옷걸이에 건 옷,
배경이 제각각인 폰 사진이다. 스튜디오 컷이 아니다.
→ 차가운 회색이나 순백 위에 놓으면 사진의 잡티가 그대로 드러난다.
**따뜻한 종이색 바탕**이 배경이 제각각인 사진들을 하나의 톤으로 묶어 준다.

**전제 2. 윈도우에서 본다.** 기본 한글 폰트는 맑은 고딕이고 웨이트가 사실상 400/700 둘이다.
→ 얇은 폰트의 우아함, 다섯 단계 웨이트 위계는 **거기서 무너진다.**
위계는 **크기 대비와 여백**으로 만든다. 굵기는 두 단계만 쓴다.

**전제 3. 매일 여는 앱이다.** 화려한 장식은 사흘이면 질린다.
→ 장식을 넣지 않고, 대신 **기록이 쌓이는 것 자체가 장식**이 되게 한다.
LOOK 번호가 늘어나고 날짜가 쌓이는 것이 이 앱의 시각적 보상이다.

---

## 색

따뜻한 뉴트럴. 채도 있는 색은 **AI 가 개입한 자리에만** 쓴다.
그래서 사용자는 색만 보고도 "이건 AI 가 한 것"을 안다. 원본 Orbit 이 퍼플 도트로 하던 일을 규칙으로 굳힌 것이다.

```css
/* 라이트 (기본) */
--paper:        #FAF7F2;  /* 배경. 순백이 아니라 미색 종이 */
--paper-sunk:   #F1EDE6;  /* 눌린 면, 사진 뒤 바닥 */
--card:         #FFFFFF;  /* 카드. 종이 위에 얹힌 한 겹 */
--ink:          #17150F;  /* 본문. 순검정 아님, 따뜻한 먹 */
--ink-soft:     #6B6459;  /* 보조 텍스트 */
--ink-faint:    #6E6659;  /* 라벨, 비활성. 11px 로 쓰이므로 대비가 기준이다.
                             paper 5.30:1 · paper-sunk 4.85:1 · card 5.66:1 */
--rule:         #E4DED4;  /* 구분선 */

--accent:       #5B4FD6;  /* AI 전용. 이 색이 보이면 AI 가 개입한 것 */
--accent-soft:  #EEEBFC;
--danger:       #A3402F;
--danger-soft:  #F7EAE7;

/* 다크 */
--paper:        #14120F;
--paper-sunk:   #0D0C0A;
--card:         #1E1B17;
--ink:          #F2EEE7;
--ink-soft:     #A8A196;
--ink-faint:    #8C8377;  /* 라이트값을 그대로 뒤집으면 소형 텍스트가 안 읽힌다.
                             paper 5.01:1 · paper-sunk 5.24:1 · card 4.60:1 */
--rule:         #2C2822;
--accent:       #9A90F5;
--accent-soft:  #23204A;
```

**규칙**
- 액센트로 **면을 덮지 마라.** 텍스트·아이콘·1~2px 선·작은 도트에만.
- 주요 버튼은 액센트가 아니라 **먹(`--ink`)** 이다. 화면에서 가장 강한 것은 사진이어야 하고,
  그다음이 검은 버튼이다. 보라색 큰 버튼은 사진과 싸운다.
- 상태색(성공/경고)을 새로 만들지 마라. 먹과 위험색으로 충분하다.

---

## 타이포

**웨이트는 400 과 700 둘만 쓴다.** 맑은 고딕에 300·500·600 이 없다.
있는 척하면 윈도우에서 전부 400 으로 뭉개진다.

```css
--font: "Pretendard Variable", Pretendard, -apple-system, BlinkMacSystemFont,
        "Apple SD Gothic Neo", "Segoe UI", "Malgun Gothic", "맑은 고딕", sans-serif;
--font-num: "SF Mono", "Consolas", ui-monospace, monospace;  /* 번호·날짜 전용 */
```

| 역할 | 크기 | 웨이트 | 자간 | 비고 |
|---|---|---|---|---|
| 화면 제목 | 30px | 700 | -0.03em | 한글은 자간을 조여야 덩어리로 읽힌다 |
| 섹션 제목 | 20px | 700 | -0.02em | |
| 카드 제목 | 17px | 700 | -0.02em | |
| 본문 | 15px | 400 | -0.01em | 줄높이 1.65 |
| 보조 | 13px | 400 | 0 | `--ink-soft` |
| **인덱스 라벨** | 11px | 700 | **0.14em** | 대문자 영문. `LOOK 014`, `WARDROBE`, `ARCHIVE` |
| 번호·날짜 | 12px | 400 | 0.06em | `--font-num`, `--ink-faint` |

**인덱스 라벨이 이 디자인의 서명이다.** 넓은 자간의 작은 대문자 영문이 한글 제목 위에 놓인다.
원본의 "Coordination Details" 를 규칙으로 승격시킨 것이다. 모든 섹션 머리에 하나씩 둔다.

---

## 간격 · 형태

```css
--s1: 4px;  --s2: 8px;   --s3: 12px;  --s4: 16px;
--s5: 24px; --s6: 32px;  --s7: 48px;  --s8: 72px;
--r-img:  20px;   /* 사진 */
--r-card: 18px;   /* 카드 */
--r-btn:  12px;
--r-pill: 999px;
```

- 섹션 사이는 `--s7` 이상. **여백이 위계다.** 촘촘하면 저널이 아니라 대시보드가 된다.
- 카드 안쪽 여백은 `--s5`.
- 그림자는 거의 쓰지 않는다. 쓸 때는 `0 1px 2px rgba(23,21,15,.05)` 정도.
  **떠 보이게 하지 말고 종이 위에 놓인 것처럼.**
- 테두리는 `1px solid var(--rule)` 하나만.

---

## 사진

이 앱에서 가장 중요한 규칙이다.

- **비율 고정**: 옷 `4:5` 세로, 가상 착용 `3:4`. `object-fit: cover`.
- 사진 뒤에는 항상 `--paper-sunk` 바닥을 깐다. 로딩 중에도 빈 구멍이 보이지 않는다.
- 사진 위에 **안쪽 테두리** `inset 0 0 0 1px rgba(23,21,15,.06)` 를 얹어
  밝은 배경의 사진이 카드와 경계 없이 번지는 것을 막는다.
- 사진이 없는 옷: 이모지 금지. `--paper-sunk` 바닥 + 가운데에 **카테고리 이니셜**
  (`TOP` / `BTM` / `OUT`) 을 인덱스 라벨 스타일로. 조용하고 품위 있다.
- 로드되면 `opacity 0 → 1`, 240ms.

---

## 아이콘

- **이모지 금지.** 전부 인라인 SVG.
- 선 굵기 `1.5px`, 라운드 캡, `currentColor`, 24px 그리드.
- 필요한 것: 홈, 옷장, 추가(+), 기록, 설정, 하트(빈/찬), 뒤로, 닫기, 카메라, 스파클(AI), 휴지통.
- **스파클만 액센트색**을 쓴다. AI 를 뜻하는 유일한 아이콘이기 때문이다.

---

## 모션

```css
--ease: cubic-bezier(.32,.72,0,1);
--fast: 140ms;  --base: 240ms;  --slow: 380ms;
```

- 화면 전환: 8px 상승 + 페이드, `--base`.
- 시트: 아래에서 올라옴, `--slow`.
- 눌림: `scale(.98)`, `--fast`.
- 가상 착용 생성 중에는 **진행 감각**을 준다. 스피너 하나로 30초를 버티게 하지 마라.
- `prefers-reduced-motion: reduce` 면 전부 끈다.

---

## 핵심 컴포넌트

**LOOK 카드** — 이 앱의 얼굴.
```
LOOK 014                    2026.08.13     ← 인덱스 라벨 + 번호(mono)
╭──────────────────────────────────────╮
│                                      │
│              사진 3:4                 │
│                                      │
│  ● AI 추천                     ♡     │  ← 좌하단 pill, 우하단 하트
╰──────────────────────────────────────╯
단정한 출근룩                              ← 카드 제목
셔츠 · 슬랙스 · 코트                        ← 보조
```
- pill: 반투명 흰 배경 + 블러, 앞에 액센트 도트 하나.
- 사진이 없으면(가상 착용 전) 구성 옷 사진들을 **가로로 이어 붙인 스트립**으로 대체한다.

**옷 카드** — 그리드 2열(모바일) / 3~4열(데스크톱). 사진 4:5 + 이름 + 색·소재 한 줄.
테두리 없이 사진 자체가 카드다.

**빈 상태** — 이 앱에서 두 번째로 중요한 화면.
큰 여백, 인덱스 라벨, 한 문장 설명, 행동 하나. 목록이나 3단계 안내를 늘어놓지 마라.

---

## 금지

- 이모지를 UI 요소로 쓰는 것
- 액센트색으로 면을 덮는 것
- 그라디언트, 네온, 글래스모피즘
- 400/700 이외의 웨이트
- `alert()` / `confirm()`
- "로컬", "개인용", "데모" 같은 표현이 사용자에게 보이는 것
- 이 문서에 없는 색·간격 값을 즉흥으로 만드는 것
