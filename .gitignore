.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
.idea/
*.iml
.DS_Store
data/
*.db
.env

# 포장 산출물. CI 가 여기에 zip/msi 를 모으고, 손으로 jpackage 를 돌려볼 때도 쓴다.
# 100MB 가 넘는 물건들이라 저장소에 들어갈 자리가 아니다 — 배포는 CI 아티팩트로 한다.
dist/

# 코틀린 컴파일러가 빌드마다 만드는 세션 디렉터리. 빌드를 한 번 돌리면 `git status`
# 에 나타나서, 옆에서 작업하는 사람이 자기 변경인가 하고 한 번 확인하게 된다.
.kotlin/
